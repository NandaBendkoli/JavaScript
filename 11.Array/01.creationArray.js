
// array is special variable that can hold multiple values in single place 

// creating array
// 1. using squre bracket
let arr = [1,2,3,4,5];

// using a array constructor (new Keyword)

let arr2 = new Array(1,2,3,4,5);
// but it is not recommended because it behavees weirdly with one number

let arr3 = new Array(3); 
// this will create an aaray of length 3 with undefined value
// arr3 =["undefined","undefined","undefined"]
// this does not create an array with single value which is arr3 = [3];

// thats why the best way to createan array is using square bracket
